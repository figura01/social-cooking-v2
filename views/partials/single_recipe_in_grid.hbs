<li class="grid list item">
    <div class="card">
        <img src="{{this.image}}" />
        <div class="card-content">
            <h3>{{this.title}}</h3>

            <div class="actions">


                {{#if isLoggedIn}}
                <a href="/recipes/{{this._id}}/my" class="btn btn-more">
                    See this recipe
                </a>

                <button class="btn btn-add-favorite">Add to Favorite</button>
                {{else}}
                <a href="/recipes/{{this._id}}/showone" class="btn btn-more">
                    See this recipe
                </a>
                {{/if}}
            </div>
        </div>
    </div>
</li>